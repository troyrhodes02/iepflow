# IEPFlow API — TypeScript Scaffold

## Scripts
- dev: run TypeScript directly via tsx
- build: compile TS to dist/
- start: run compiled JS (production)
- typecheck: TS type checks without emit
- test: runs Vitest (no tests yet)
